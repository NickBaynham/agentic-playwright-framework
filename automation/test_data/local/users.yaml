# SauceDemo demo accounts.
#
# These credentials are publicly published on the saucedemo.com landing page
# and are intended for unrestricted demo use. They are committed only because
# saucedemo publishes them itself. Do not extend this file with private or
# production credentials.

users:
  standard_user:
    username: standard_user
    password: secret_sauce
    role: standard
  locked_out_user:
    username: locked_out_user
    password: secret_sauce
    role: locked_out
  problem_user:
    username: problem_user
    password: secret_sauce
    role: problem
  performance_glitch_user:
    username: performance_glitch_user
    password: secret_sauce
    role: performance_glitch
  error_user:
    username: error_user
    password: secret_sauce
    role: error
  visual_user:
    username: visual_user
    password: secret_sauce
    role: visual
